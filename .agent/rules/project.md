# Workspace Rule

Follow the canonical project instructions in `../../AGENTS.md`.

Use the project skills in `.agents/skills/` when a matching thesis, research, document, PDF, Office, Markdown, LaTeX, diagram, or slide task is requested.
